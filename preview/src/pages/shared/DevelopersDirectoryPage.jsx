import UsersDirectoryPage from './UsersDirectoryPage';

export default function DevelopersDirectoryPage() {
  return (
    <UsersDirectoryPage
      forcedRole="developer"
      title="Developers"
      subtitle="Sellers and listings. Click a name for projects, sales and earnings."
    />
  );
}
